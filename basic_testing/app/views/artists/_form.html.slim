= form_for @artist, html: { id: 'artist-form' } do |f|
  = f.label :name
  = f.text_field :name

  div class='form-errors' id='name-errors'
    - @artist.errors.messages[:name].each do |message|
      div = message

  = f.submit title
