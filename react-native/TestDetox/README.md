```bash
yarn eslint --init

yarn add -D eslint-plugin-react-native

yarn add -D babel-plugin-module-resolver eslint-import-resolver-typescript

yarn add -D prettier eslint-config-prettier eslint-plugin-prettier
```
